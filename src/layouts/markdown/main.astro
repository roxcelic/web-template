---
interface Props {
	title: string;
    desc: string;
}

const { desc } = Astro.props;
const { title } = Astro.props;

import Layout from '../full.astro'
---
<Layout desc={ desc } title={ title }>
	<div style="width: 100%; margin:auto;text-align: left;">
		<slot />		
	</div>
</Layout>